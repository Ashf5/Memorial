{
  "name": "memorial_website",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server/client && npm install --prefix server",
    "build": "npm run build --prefix server/client && npm run build --prefix server",
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server/client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
